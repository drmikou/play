# This is the main configuration file for the application.
# ~~~~~

# Secret key
# ~~~~~
# The secret key is used to secure cryptographics functions.
#
# This must be changed for production, but we recommend not changing it in this file.
#
# See http://www.playframework.com/documentation/latest/ApplicationSecret for more details.
play.crypto.secret=">YKCc]:Qb7c>JETNymk66Xi1736nuGus/Sj1:XL=`^@>?]`04<gtL3c2Bipt<hqi"

# The application languages
# ~~~~~
application.langs="en"

# Global object class
# ~~~~~
# Define the Global object class for this application.
# Default to Global in the root package.
# application.global=Global

# Router
# ~~~~~
# Define the Router object to use for this application.
# This router will be looked up first when the application is starting up,
# so make sure this is the entry point.
# Furthermore, it's assumed your route file is named properly.
# So for an application router like `conf/my.application.Router`,
# you may need to define a router file `my.application.routes`.
# Default to Routes in the root package (and `conf/routes`)
#application.router=Routes
play.http.router=root.Routes

#parsers.text.maxLength=100M
play.http.parser.maxMemoryBuffer=100M

ldapix.url = "http://ldapix.juniorisep.com"
ldapix.iv = "ZGI3NGQ0Mjc5M2FN"
ldapix.key = "Excellence452015"

# Database configuration
# ~~~~~
# You can declare as many datasources as you want.
# By convention, the default datasource is named `default`
#

# Development configuration, database isn't persisted
//db.default.driver=org.h2.Driver
//db.default.url="jdbc:h2:mem:play;MODE=PostgreSQL;LOCK_MODE=0"

db.default.driver=org.postgresql.Driver
db.default.url="jdbc:postgresql://localhost:5433/catnix-rel"
db.default.username=postgres
db.default.password="catnix"

# Production configuration

//db.default.driver=org.postgresql.Driver
//db.default.url="jdbc:postgresql://172.17.0.6/catnix-rel"
//db.default.username=postgres
//db.default.password="catnix"

#
# You can expose this datasource via JNDI if needed (Useful for JPA)
# db.default.jndiName=DefaultDS

# Evolutions
# ~~~~~
play.evolutions.db.default.autoApply=true
play.evolutions.db.default.autoApplyDowns=true
# play.evolutions.enabled=false
# You can disable evolutions if needed
# evolutionplugin=disabled

# Ebean configuration
# ~~~~~
# You can declare as many Ebean servers as you want.
# By convention, the default server is named `default`
#
 ebean.default=["models.*"]
 

# Logger - Deprecated since play 2.4
# ~~~~~
# You can also configure logback (http://logback.qos.ch/),
# by providing an application-logger.xml file in the conf directory.

# Root logger:
#logger.root=ERROR

# Logger used by the framework:
#logger.play=INFO

# Logger provided to your application:
logger.application=DEBUG


# Custom Settings
# ~~~~~~~~~~~~~~~
play {
  modules {
    enabled += be.objectify.deadbolt.java.DeadboltModule
    enabled += "modules.CustomDeadboltHook"
    enabled += "play.api.libs.mailer.MailerModule"
  }
  http {
      errorHandler = "utils.core.CustomHttpErrorHandler"
  }
}

# Mailer configuration
play.mailer {
  host = "aspmx.l.google.com"
#  mock = true
}

fromMail {
  firstname = "Catnix"
  lastname = "Automated Process"
  mail = "catnix-noreply@juniorisep.com"
}

catnix.baseUrl = "http://catnix-uat.juniorisep.com"

deadbolt {
  java {
    # cache-user is set to false, otherwise it's not possible to mix deadbolt handler that do and don't have users in the template examples
   	cache-user=false
  }
}

gitlab.reportIssues = false
